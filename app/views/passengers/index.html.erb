

<h1>Welcome to Passengers List</h1>
<p >
<%= link_to "Add New Passenger", new_passenger_path %>
</p>


<table>
  <tr>
    <th>
    ID
    </th>
    <th>
     Name
    </th>
    <th>
  Phone NO.
    </th>
    <th>

    </th>

    <th>

    </th>
  </tr>

    <% @passengers.each do |passenger| %>
   <tr>
    <td><%= passenger.id %></td>
      <td><%= link_to passenger.name, passenger_path(passenger.id) %></td>
      <td><%= passenger.phone_num %></td>

      <td> <%= button_to "Edit", edit_passenger_path(passenger.id), method: :get %></td>

      <td> <%= button_to "Delete", passenger_path(passenger.id), method: :delete, data: {confirm: "Are you sure you want to delete this"} %></td></tr>
    <% end %>
  </table>
